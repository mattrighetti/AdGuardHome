import XCTest
@testable import adguard_home_swift

final class adguard_home_swiftTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(adguard_home_swift().text, "Hello, World!")
    }
}
